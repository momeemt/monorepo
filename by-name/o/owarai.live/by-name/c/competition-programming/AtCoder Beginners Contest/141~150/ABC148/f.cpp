#include <bits/stdc++.h>
using namespace std;

int main() {
  long n,u,v;
  cin >> n >> u >> v;
  vector<int> a(n);
  vector<int> b(n);
  for (int i = 0; i < (n-1); i++) {
    cin >> a[i] >> b[i];
  }
}
