#include <bits/stdc++.h>
using namespace std;
const long long INF = -1 * (1LL << 60);

int main() {
  int n;
  int yest;
  vector<long long> a(101000);
  vector<long long> b(101000);
  vector<long long> c(101000);
  vector<long long> dp(101000);
  cin >> n;
  for (int i = 0; i < n; i++) cin >> a[i] >> b[i] >> c[i];
  for (int i = 0; i < n; i++) {

  }
}
